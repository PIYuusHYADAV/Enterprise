version: "3.9"
services:
  trading-research-assistant:
    build: .
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
